# encoding: utf-8

source 'https://rubygems.org'

# Specify your gem's dependencies in guard-xmllint.gemspec
gemspec

group :development, :test do
  gem 'devtools', git: 'https://github.com/rom-rb/devtools.git'
  eval_gemfile 'Gemfile.devtools'
end
